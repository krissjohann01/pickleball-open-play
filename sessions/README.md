# Session summaries

Saved here automatically when you click **Save Summary** on the end-of-session
screen — the server (running on whichever machine hosts the app) writes the
`.md` file directly into this folder. Works the same from any device or
browser, since the write happens on the server, not in your browser.
